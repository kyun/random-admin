import { Connection } from 'mysql2/promise';

/*
  역할
  - endpoint에 매칭된 mission 존재 여부 확인
  - mission
*/
export async function missionManager({ endpoint, user_id }: any, connection: Connection) {
  try {

  } catch (e) {

  }
}